#include <bits/stdc++.h>

using namespace std;

const long long mod = 1e9 + 7;

long long n, k, ways;

int main()
{
    long long t;

    cin >> t;

    while(t--)
    {
        cin >> n >> k;

        if(n == k)
        {
            ways = 1;
        }

        else if(k > n)
        {
            ways = 0;
        }

        else if()
        {
            for(long long r = 0; r <= k; r++)
            {
                
            }
        }

        cout << ways << "\n";
    }

    return 0;
}
